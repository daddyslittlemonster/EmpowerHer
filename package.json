{
  "name": "empowerher",
  "version": "1.0.0",
  "description": "A financial education platform for women",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd packages/frontend && npm start\" \"cd packages/backend && node src/server.js\"",
    "install-all": "npm install && cd packages/frontend && npm install && cd ../backend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "finance",
    "education",
    "women",
    "empowerment"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
